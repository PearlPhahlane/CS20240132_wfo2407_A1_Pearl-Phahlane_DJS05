//Initial state object
const initialState = {
    count = 0
};

//store object
const store = (() => {
    //private variables for state and subscribers
    let state = initialState;
    const subscribers = [];

    
})